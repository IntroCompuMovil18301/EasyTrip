<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.michael.taller1_fibo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Principal">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".posicionesFibo" />
        <activity android:name=".calculoFacto" />
        <activity android:name=".calculoFibo" />
        <activity
            android:name="example.michael.taller1fibonacci.Navegador"
            android:label="@string/title_activity_navegador"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name="example.michael.taller1fibonacci.paises" />
        <activity android:name="example.michael.taller1fibonacci.pais"></activity>
    </application>

</manifest>